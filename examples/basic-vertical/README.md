# example-basic-vertical

A minimal Snapdeck demo with four vertically stacked sections.

## Run

From the monorepo root:

```sh
pnpm install
pnpm --filter example-basic-vertical dev
```

Then open the Vite URL printed in the terminal.

## Build

```sh
pnpm --filter example-basic-vertical build
```
